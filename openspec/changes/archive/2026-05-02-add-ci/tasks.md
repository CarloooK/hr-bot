# Tasks: Add CI Pipeline with Tests

- [x] 1. 编写 tests/test_hr_bot.py — 4 个测试用例（/health, /api/stats, /api/version, /api/config）
- [x] 2. 编写 pytest.ini — 测试配置
- [x] 3. 编写 .github/workflows/ci.yml — GitHub Actions CI 配置
- [x] 4. 更新 requirements.txt — 添加 pytest 依赖
- [x] 5. 本地运行测试确认全部通过（pip install httpx + pytest 后）
- [ ] 6. commit + push 触发 GitHub Actions
- [ ] 7. 在 GitHub Actions 页面确认 CI 运行结果（绿色 pass）
